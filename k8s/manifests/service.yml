apiVersion: apps/v1
kind: Service
metadata:
  name: go-wiki-service
  labels:
    app: go-wiki
spec:
  selector:
    app: go-wiki
  type: ClusterIP
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
